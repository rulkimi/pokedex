import SafariClient from "./_components/safari-client";

export const metadata = {
	title: "Safari Zone | Pokédex by rulkimi",
	description: "Catch wild roaming Pokemon in the Safari Zone!"
};

export default function SafariPage() {
	return <SafariClient />;
}
